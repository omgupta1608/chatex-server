/**
 * additional login/register option buttons
 * @param {Object} props
 * @param {string} props.prefix ${prefix} with Google etc.
 */
const OrButtons = ({ prefix }) => {
	// TODO: GoogleButton
	return (
		<>
			<div className='or-seperator'>
				<span>OR</span>
			</div>
			{/* GoogleButton */}
			{/* can add other options */}
		</>
	);
};

export default OrButtons;
